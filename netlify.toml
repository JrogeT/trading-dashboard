[build]
  command = "npm run build"
  publish = "dist/trading-dashboard/browser"

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = "dist/"
  SECRETS_SCAN_OMIT_KEYS = "SUPABASE_KEY,SUPABASE_URL,BINANCE_WS_URL"
